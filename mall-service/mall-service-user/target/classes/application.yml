spring:
  profiles:
    active: dev
    include: feign
  application:
    name: mall-service-user
  cloud:
    nacos:
      server-addr: localhost:8848
      username: nacos
      password: nacos
      config:
        namespace: dev
        username: nacos
        password: nacos




server:
  port: 9000
# JWT 签名密钥（生产环境请通过环境变量注入，避免硬编码在仓库中）
jwt:
  secret: Ai+QwjUslNR6e0NXAcvbhv4LxGsLqkMlrq980le2VDPuwc+USgBW59fnAg9+

logging:
  level:
    com.hyx.order.feign: debug
---
spring:
  config:
    activate:
      on-profile: test
    import:
      - nacos:common.yaml?group=mall-service-user
      - nacos:datasource.yaml?group=mall-service-user

---
spring:
  config:
    activate:
      on-profile: dev
    import:
      - nacos:common.yaml?group=mall-service-user
      - nacos:datasource.yaml?group=mall-service-user

---
spring:
  config:
    activate:
      on-profile: prod
    import:
      - nacos:common.yaml?group=mall-service-user
      - nacos:datasource.yaml?group=mall-service-user

