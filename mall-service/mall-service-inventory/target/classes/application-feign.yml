spring:
  cloud:
    openfeign:
      client:
        config:
          default:
            logger-level: FULL
            connect-timeout: 1000
            read-timeout: 2000
          ServiceProduct:
            logger-level: FULL
            connect-timeout: 3000
            read-timeout: 5000
#            request-interceptors:
#              - com.hyx.order.interceptor.XTokenInterceptor
    sentinel:
      transport:
        dashboard: localhost:8080
      eager: true
      web-context-unify: false
feign:
  sentinel:
    enabled: true